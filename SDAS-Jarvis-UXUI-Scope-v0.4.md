# SDAS「Jarvis」AI 平台 — UX／UI 設計範圍表（v0.4 · 供報價用）

| 項目 | 內容 |
|---|---|
| 專案 | 對話式 AI 助理平台（全繁體中文）之 UX 流程與 UI 設計 |
| 設計範圍 | 資訊架構與導覽收斂、各模組使用流程定義、畫面版面與狀態設計、互動規範、設計系統與元件庫建置、手機版適配 |
| 不含 | 前後端實作、API 規格、資料串接邏輯 |
| 產出形式 | Figma 設計檔（畫面、元件庫、規格說明頁）＋設計規範文件 |

---

## 1 · 資訊架構與導覽（IA）

| 設計項目 | 設計內容 |
|---|---|
| 導覽收斂 | 原 7 頁導覽重新盤點並收斂為 5 格（對話／知識庫／監控／排程與報告／設定），提出收斂方案畫板與取捨說明 |
| 分類判準定義 | 訂出「工作流 vs 設定」的頁面歸屬判準，供後續新增頁面沿用 |
| 共用外殼設計 | icon rail ＋ 側欄 ＋ 頂部 header ＋ 內容區的版面骨架與尺寸定義 |
| 側欄模式定義 | 統一「側欄＝切換器、主區＝單一詳情」的兩層心智模型，套用於知識庫與監控 |
| 側欄行為規範 | 側欄只放檢視、不放動作；新增類動作一律移至主區右上按鈕 |

---

## 2 · 對話 Chat

**畫面**：主對話、生成中、圖表往返、（手機）長按選單

| 設計項目 | 設計內容 |
|---|---|
| 對話流程定義 | 提問 → 生成 → 回覆 → 追問／回溯的完整流程與各節點畫面狀態定義 |
| 訊息 UI 設計 | 訊息泡泡 5 種狀態設計（使用者／AI／hover 操作列／排隊／思考中），含對齊、留白、時間戳位置規範 |
| Markdown 回覆樣式定義 | AI 回覆的 9 種內容區塊樣式（表格／清單／編號清單／程式碼／Mermaid／引用／H2／H3／圖表），並統一各區塊的複製與下載按鈕圖示、字級、對位規則 |
| 輸入區 Composer 設計 | 四種狀態版面（可送出／未輸入／生成中／超過高度上限）；定義生成中的中斷行為、續打與訊息排隊的互動規則、取消排隊的回退路徑；格式輸入改為純打字快捷（放棄格式工具列），並定義送出與換行按鍵 |
| 附件流程與樣式 | 附件選單設計、支援格式定義、不支援格式的前置阻擋流程；附件 chip 四型樣式（檔案／圖片／錯誤／圖表）與多檔換行排列規則 |
| 思考過程 UI | 可收合的思考步驟列表設計：摘要列、累計耗時位置、步驟兩種狀態圖示、收合態版面 |
| 提問目錄設計 | 右側索引面板展開／收合兩態、項目三種閱讀狀態、上限筆數與溢位處理 |
| 歷史對話清單設計 | 三種狀態（進行中／完成未讀／已讀）以顏色語言區分的樣式定義 |
| 出處引用樣式 | 出處 chip 樣式與點擊後開啟原文並高亮的互動定義 |
| 監控↔對話往返流程 | 圖表帶入對話（含時間區間顯示規則）與圖表存回 Dashboard 的雙向流程設計，含來源標示與回溯路徑 |

---

## 3 · 知識庫 RAG

**畫面**：知識庫詳情、空狀態引導、知識庫總覽、文件庫管理、檢索測試、發布與版本紀錄

| 設計項目 | 設計內容 |
|---|---|
| 兩層架構設計 | 側欄切換器＋主區單一詳情的版面設計，含停用知識庫的呈現規則 |
| 知識庫詳情版面 | 變更狀態列／說明／啟用開關／載入預算／文件清單／檢索測試／健康狀態的區塊排序與版面 |
| 總覽頁設計 | 格狀卡片版面，定義卡片必顯欄位 |
| 空狀態引導流程 | 零知識庫時的三步驟引導設計＋預填範本卡 |
| 詞彙系統定義 | 將原「向量化」拆分為 狀態／設定／動作 三套用語並全檔統一（含「切塊」改「分段」） |
| 載入方式標籤設計 | 兩種載入方式的標籤樣式與色系分工；「排除」改以獨立狀態 badge 表達的決策與樣式 |
| 成本可視化設計 | 全文載入的用量計量條設計與擺放位置（決策當下可見） |
| 文件卡設計 | 文件卡版面、三顆固定操作的分工定義、其他操作選單項目、AI 檔案說明欄位 |
| 檔案生命週期流程 | 待處理 → 處理中 → 就緒／解析失敗 各階段的畫面與狀態設計 |
| 檢索測試介面 | 查詢輸入與命中結果卡設計（片段／相似度／來源）＋命中品質回饋列 |
| 變更與版本流程 | 變更標記三型樣式、發布前差異清單設計、版本紀錄與回滾流程 |
| 對話框與抽屜 | 新增／停用／刪除知識庫對話框、知識庫與檔案兩組操作選單、檔案預覽抽屜、版本紀錄抽屜 |

---

## 4 · 監控 Dashboard

**畫面**：Dashboard 詳情、資料源管理

| 設計項目 | 設計內容 |
|---|---|
| 監控頁架構設計 | 對照已上線但未設計過的 Grafana／Prometheus 頁面，重新設計為側欄清單＋單一 Dashboard 詳情；資料源管理收為次視圖；並界定與未來機房總覽的範圍分工 |
| 側欄清單設計 | Dashboard 項目四態設計（含資料源離線態的圖示語意）＋新增按鈕＋資料源管理連結 |
| 標題列設計 | 名稱、公開徽章、改名、超標面板摘要、更新時間、時間範圍／時區／自動刷新、重新整理與操作選單的排列 |
| 統計卡設計 | 三級告警樣式（正常／警告／危險），定義超標時的變色規則與門檻值文字備援 |
| 圖表面板設計 | 面板卡版面（標題／圖表／座標軸／圖例）＋四種狀態設計：正常、此範圍無資料、類型不支援（保留原始查詢）、查詢中骨架；並定義「面板層級無資料」與「資料源離線」的呈現分工 |
| 面板操作設計 | 常駐追問圖示與操作選單的分工規則（避免同一動作出現兩個入口），兩層選單項目定義 |
| 新增面板流程 | 兩個入口設計（網格虛線佔位、操作選單）；自訂圖表與貼上 JSON 兩條輸入方式以分段控制切換、共用同一份即時預覽的流程設計；含查詢驗證的行內回饋三態 |
| 圖表樣式設定 | 色盤選擇（三套＋自訂 hex）、格線、圖例位置的設定介面；定義閾值色不納入樣式設定的分界 |
| 建立 Dashboard 流程 | 三條路徑（範本／Grafana JSON／AI Prompt 生成）以分段控制整合、共用欄位與建立摘要；JSON 貼上後的就地解析摘要設計；無可用資料源時的守門狀態與引導 |
| 資料源管理設計 | 資料源清單卡＋新增資料源對話框（連線測試與驗證結果三態、進階設定摺疊、預設資料源設定） |
| 破壞性操作設計 | 刪除 Dashboard 與刪除資料源的確認層級分級，及影響範圍的告知方式 |
| Grafana 匯入視覺規範 | 定義「語意取自客戶 JSON、視覺一律套 SDAS 樣式」的分工，含閾值色例外與不支援面板顯性佔位規則 |

---

## 5 · 排程與報告

**畫面**：排程（計劃列表）、報告清單、報告檢視

| 設計項目 | 設計內容 |
|---|---|
| 模組架構收斂 | 原四個視圖盤點後收斂為 排程／報告 兩格，並提出收斂理由與取捨 |
| 計劃列設計 | 一列同時呈現排程設定與其產出的版面（白話排程、cron chip、上次／下次執行、失敗原因、最新報告、開關、操作選單）；未排程草稿併入同一清單的呈現方式 |
| 提示詞可視化 | 將執行提示詞常駐於計劃列的版面設計與理由 |
| 排程設定介面 | 簡易設定／Cron 表達式 兩種輸入方式的切換設計，欄位隨頻率變動的規則，以及共用同一份「白話翻譯＋接下來三次執行」預覽的設計 |
| 報告清單設計 | 搜尋與三組篩選、分批載入取代分頁；失敗執行仍保留一列的呈現規則與可用動作差異 |
| 報告檢視設計 | 報告本文以既有 Markdown 區塊渲染的版面、出處引用、右上動作區 |
| 對話框 | 新增計劃（Cron 即時翻白話與三次執行預告）、停用、刪除三個對話框 |

---

## 6 · 設定

**畫面**：組織管理清單、組織詳情、使用者管理、組織空狀態、API 與整合

| 設計項目 | 設計內容 |
|---|---|
| 模組架構收斂 | 原三頁（管理員／Credentials／APIs）整併為單一設定頁，側欄分 組織／整合 兩組 |
| 組織清單設計 | 清單列版面（三組計數＋更新時間＋下鑽指示）與新增入口 |
| 群組管理下鑽流程 | 群組不獨立成頁、改為組織詳情下鑽的流程決策與版面（成員卡＋群組卡） |
| 加入成員流程 | 搜尋既有帳號→勾選→加入的流程設計，含已在組織者的呈現與「不在此開帳號、不在此設角色」的邊界定義 |
| 使用者管理設計 | 搜尋與角色篩選；受保護帳號與一般帳號的差異化樣式（唯讀徽章 vs 可切換欄位） |
| 空狀態設計 | 零組織時收起次要入口、只留單一主行動＋三步驟引導 |
| API 與整合頁設計 | API Key 建立與清單版面、使用說明與程式碼範例區塊、API 文件外連卡 |
| 對話框 | 新增組織／群組／使用者、加入成員、刪除組織（含輸入名稱確認）五個對話框 |
| 帳號設定歸屬 | 定義個人帳號類設定（更改密碼）移至使用者選單、不放設定頁 |

---

## 7 · 手機版適配

**畫面**：對話、對話（生成中）、長按訊息選單、報告清單、報告檢視（390×844 基準）

| 設計項目 | 設計內容 |
|---|---|
| 手機範圍界定 | 盤點桌機功能後界定手機只保留「問」與「看結果」兩條路徑，並提出取捨理由 |
| 導覽設計 | 5 格導覽改為兩格底部 tab bar，定義各頁是否顯示 tab bar |
| 版面尺寸定義 | 狀態列／標題列／內容區／輸入列／tab bar 各段固定高度與左右留白規範 |
| 對話頁適配 | 訊息由下往上堆疊的版面規則、回覆區塊寬度適配、生成中狀態適配 |
| 長按選單設計 | 遮罩＋底部 sheet 設計（圓角、拖曳把手、選項），取代桌機 hover 操作列的互動轉換 |
| 報告清單卡片化 | 桌機表格改三行卡片的資訊取捨與版面，含整列點擊與操作選單分工 |
| 篩選 chip 設計 | 下拉改一排 chip 及套用狀態的高亮樣式 |
| 底部固定動作列 | 主要動作由右上移至底部固定列的版面設計 |
| 手機元件 | 手機專用 Header、Status Bar、報告卡、篩選 chip、tab bar 五個元件 |

---

## 8 · 設計系統與元件庫

| 設計項目 | 設計內容 |
|---|---|
| 色彩系統 | 品牌色、介面色、狀態色與漸層的 token 定義（Icon Rail／Sidebar／Header／Page BG／Primary／Accent／Border／Ink／Muted／Warn／Code） |
| 文字系統 | Inter 字型五級字階定義（Title／Section／Body／Label／Caption）與行高 |
| 版面規範 | 圓角、卡片 padding 與邊框、對話框寬度與高度上限（超過改中間捲動）等共用規則 |
| 顏色語意分層規範 | 定義設定／事實／變更／diff／中性資訊各自的表現形式，避免色彩語意互相佔用；並訂出「狀態色必須搭配文字」的無障礙規則 |
| 元件庫建置 | 共 30+ 個 Figma Component（含 Variants 與布林屬性），詳如下表 |
| 規格說明頁 | 各模組的 SPEC 說明頁：真實 UI 零件＋編號圖釘＋文字說明，統一格式與字級 |

### 元件清單

| 元件 | 變體／屬性 |
|---|---|
| Button | 4 型 × 3 態＝12 變體 |
| Sidebar／Side Menu Item | 3 態容器＋6 變體選單項，含 hover 展開互動 |
| Main Header／Card／Input／Warning Box | Input 3 態 |
| Dialog | 3 種層級＋「需輸入名稱」布林屬性 |
| Dropdown Menu／Menu Item | 通用容器＋2 態選項；具名版本 5 組 |
| Message Bubble | 5 種狀態＋收合屬性 |
| Composer | 4 態 |
| MD Block | 9 型 |
| Attachment Chip | 4 型 |
| 提問目錄 TOC／TOC Item | 2 態／3 態 |
| History Item | 3 態＋文字屬性 |
| KB List Item／KB Card／Mode Tag／Doc Card | 4 態／2 態／2 型／含布林屬性 |
| Context Budget／Template Card／Hit Card／Version Item／Source Chip | 各 1 |
| Change Flag／Diff Row／Toggle | 3 型／3 型／3 態 |
| Dashboard List Item／Stat Card／Chart Panel／Panel Placeholder | 4 態／3 級／4 態＋布林屬性／1 |
| 手機專用元件 | 5 個 |
| 規格註記元件 | specInfo／Annotation Note／Annotation Pin |

---

## 9 · 待確認事項

| 項目 | 說明 |
|---|---|
| Persona | 主要使用者為工程師自用或管理者看報告，影響手機版與報告模組的設計優先序 |
| 權限模型 | 「公開／私人」在 Dashboard、資料源、知識庫各出現一次，公開範圍（全組織／特定群組）尚未定義 |
| Dashboard 範本內容 | 現有三組範本需客戶確認是否為實際在用的 dashboard |
| AI 生成 Dashboard 邊界 | 生成後可否編輯、查詢驗證方式、生成失敗的回饋設計尚未定案 |
| 資料呈現規則 | 監控與統計目前為示意值，正式串接後的刷新頻率、逾時與失敗狀態需補設計 |
| 舊頁面 | 導覽收斂後原五個獨立頁面已無對應畫板，若需逐項比對需重畫 |
| 兩處規格不一致 | 送出快捷鍵與輸入框高度上限在設計檔內記載不一，需擇一定案 |

---

## 10 · 未列入本次設計範圍

| 項目 | 說明 |
|---|---|
| 機房總覽 Dashboard | 機櫃／電力／溫濕度等實體指標，與服務層級監控分屬兩張畫面 |
| 主動建議提問 | AI 主動提示下一步該問什麼 |
| AI 主動排錯與流程編輯 | 讓 AI 主動找錯並可編輯自動排錯流程 |
| 事件觸發自動處理／通報 | 資安風險較高，可能限縮為自動通報 |
| 報告 email 發送、公開報告群組權限 | 需先定義權限模型 |
| AI 工具管理與狀態觀看 | API／MCP／CLI 三類工具的開關與健康狀態 |
| 前後端實作、API 規格、計費邏輯 | — |
